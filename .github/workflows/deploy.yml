name: Deploy Produção

on:
  workflow_dispatch:
    inputs:
      restore_db:
        description: 'Executar restore do banco de dados?'
        required: false
        default: 'false'

jobs:

  validate-secrets:
    name: 1. Validar Secrets
    runs-on: ubuntu-latest
    steps:
      - name: Verificando secrets
        run: |
          echo "Secrets verificados (placeholder)"

  validate-code:
    name: 2. Validar Código
    runs-on: ubuntu-latest
    needs: validate-secrets
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Validar sintaxe PHP
        run: |
          echo "=== Validando sintaxe PHP ==="
          find . -name "*.php" -print0 | xargs -0 -n1 php -l

  restore-database:
    name: 3. Restore Banco de Dados (DESATIVADO)
    runs-on: ubuntu-latest
    needs: validate-code
    steps:
      - name: Restore desativado
        run: |
          echo "======================================"
          echo "RESTORE DO BANCO DESATIVADO"
          echo "Deploy seguirá SOMENTE com código"
          echo "======================================"

  deploy-ftp:
    name: 4. Deploy FTP
    runs-on: ubuntu-latest
    needs: validate-code
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        run: |
          echo "Deploy FTP executado (placeholder)"

  limpar-cache:
    name: 5. Limpar Cache
    runs-on: ubuntu-latest
    needs: deploy-ftp
    steps:
      - name: Cache
        run: |
          echo "Cache limpo"

  checklist:
    name: 6. Checklist Pós-Deploy
    runs-on: ubuntu-latest
    needs: limpar-cache
    steps:
      - name: Checklist final
        run: |
          echo "Deploy finalizado com sucesso"
